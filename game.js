var canvas = document.getElementById("canvas1");
var cxt = canvas.getContext("2d");

export default class Game {
    constructor() {
        this.menu = true;
        this.gameOver = false;
        this.session = false; 
    }

    init() {
        if (this.menu) {
            
        }
    }

    

}