
export default class CustomAudio {
    // array of file paths as arg:
    constructor(paths) {
        this.audioContext;
        this.samples = samples;
        this.paths = paths;
        

    }
}